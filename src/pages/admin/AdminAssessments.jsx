import { useMemo, useState } from 'react'
import { ClipboardList, Plus, Clock3, Users } from 'lucide-react'
import { useApp } from '../../context/AppContext'

const ASSESSMENTS_KEY = 'educheck_assessments'

export default function AdminAssessments() {
  const { assessmentHistory } = useApp()
  const [assessments, setAssessments] = useState(() => JSON.parse(localStorage.getItem(ASSESSMENTS_KEY) || '[]'))
  const [showModal, setShowModal] = useState(false)
  const [form, setForm] = useState({ title: '', grade: 'Grade 5', duration: '30', status: 'Draft' })
  const latestAttempts = useMemo(() => (assessmentHistory || []).slice().reverse().slice(0, 8), [assessmentHistory])

  const saveAssessment = (event) => {
    event.preventDefault()
    if (!form.title.trim()) return
    const next = [...assessments, { id: Date.now(), ...form, title: form.title.trim(), createdAt: new Date().toISOString() }]
    setAssessments(next)
    localStorage.setItem(ASSESSMENTS_KEY, JSON.stringify(next))
    setForm({ title: '', grade: 'Grade 5', duration: '30', status: 'Draft' })
    setShowModal(false)
  }

  return <div className="space-y-6"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-center"><div><h1 className="text-3xl font-bold text-slate-900">Assessments</h1><p className="mt-2 text-slate-600">Create and manage student assessments</p></div><button onClick={() => setShowModal(true)} className="inline-flex items-center justify-center gap-2 rounded-lg bg-gradient-to-r from-sky-500 to-blue-600 px-5 py-3 font-semibold text-white"><Plus size={19} /> Create Assessment</button></div><div className="grid gap-4 sm:grid-cols-3"><div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><ClipboardList className="text-sky-600" /><p className="mt-4 text-sm text-slate-500">Configured tests</p><p className="mt-1 text-3xl font-bold text-slate-900">{assessments.length}</p></div><div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><Users className="text-emerald-600" /><p className="mt-4 text-sm text-slate-500">Completed attempts</p><p className="mt-1 text-3xl font-bold text-slate-900">{assessmentHistory?.length || 0}</p></div><div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><Clock3 className="text-amber-500" /><p className="mt-4 text-sm text-slate-500">Draft assessments</p><p className="mt-1 text-3xl font-bold text-slate-900">{assessments.filter((item) => item.status === 'Draft').length}</p></div></div><section className="rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="border-b border-slate-100 p-5"><h2 className="text-lg font-bold text-slate-900">Configured Assessments</h2></div>{assessments.length === 0 ? <p className="p-10 text-center text-sm text-slate-500">No assessments configured. Create the first assessment above.</p> : <div className="divide-y divide-slate-100">{assessments.map((item) => <div key={item.id} className="flex flex-wrap items-center justify-between gap-4 p-5"><div><p className="font-semibold text-slate-900">{item.title}</p><p className="mt-1 text-sm text-slate-500">{item.grade} · {item.duration} minutes</p></div><span className={`rounded-full px-3 py-1 text-xs font-semibold ${item.status === 'Published' ? 'bg-emerald-100 text-emerald-700' : 'bg-amber-100 text-amber-700'}`}>{item.status}</span></div>)}</div>}</section><section className="rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="border-b border-slate-100 p-5"><h2 className="text-lg font-bold text-slate-900">Recent Attempts</h2></div>{latestAttempts.length === 0 ? <p className="p-10 text-center text-sm text-slate-500">Student attempts will appear here after tests are submitted.</p> : <div className="divide-y divide-slate-100">{latestAttempts.map((item, index) => <div key={item.submittedAt || index} className="flex flex-wrap items-center justify-between gap-3 p-5"><div><p className="font-semibold text-slate-900">{item.studentName || 'Student'}</p><p className="text-sm text-slate-500">{item.subject || 'Mathematics'} · {item.submittedAt ? new Date(item.submittedAt).toLocaleDateString() : 'Recently'}</p></div><span className="font-bold text-sky-600">{Number(item.percentage || 0)}%</span></div>)}</div>}</section>{showModal && <div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-900/40 p-4 backdrop-blur-sm"><form onSubmit={saveAssessment} className="w-full max-w-lg space-y-4 rounded-2xl bg-white p-6 shadow-2xl"><h2 className="text-2xl font-bold text-slate-900">Create Assessment</h2><input required placeholder="Assessment title" value={form.title} onChange={(event) => setForm({ ...form, title: event.target.value })} className="w-full rounded-xl border border-slate-200 px-3 py-2.5" /><div className="grid gap-4 sm:grid-cols-3"><select value={form.grade} onChange={(event) => setForm({ ...form, grade: event.target.value })} className="rounded-xl border border-slate-200 px-3 py-2.5">{[1, 2, 3, 4, 5, 6, 7, 8].map((grade) => <option key={grade}>Grade {grade}</option>)}</select><input type="number" min="5" value={form.duration} onChange={(event) => setForm({ ...form, duration: event.target.value })} className="rounded-xl border border-slate-200 px-3 py-2.5" /><select value={form.status} onChange={(event) => setForm({ ...form, status: event.target.value })} className="rounded-xl border border-slate-200 px-3 py-2.5"><option>Draft</option><option>Published</option></select></div><div className="flex justify-end gap-3"><button type="button" onClick={() => setShowModal(false)} className="rounded-xl border border-slate-200 px-4 py-2.5 font-semibold">Cancel</button><button className="rounded-xl bg-sky-600 px-5 py-2.5 font-semibold text-white">Save</button></div></form></div>}</div>
}
