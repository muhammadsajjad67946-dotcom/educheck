const mysql = require('mysql2/promise'); require('dotenv').config(); async function check() { const conn = await mysql.createConnection({ host: process.env.DB_HOST || 'localhost', user: process.env.DB_USER || 'root', password: process.env.DB_PASSWORD, database: process.env.DB_NAME || 'neweducheck' }); const [rows] = await conn.query('SELECT t.id, t.name, COUNT(q.id) as count FROM topics t LEFT JOIN questions q ON t.id = q.topic_id GROUP BY t.id, t.name ORDER BY t.id'); console.log('--- Questions by Topic ---'); console.table(rows); const [total] = await conn.query('SELECT COUNT(*) as total FROM questions'); console.log('Total Questions in DB:', total[0].total); const [grades] = await conn.query('SELECT grade, COUNT(*) as count FROM questions GROUP BY grade ORDER BY grade'); console.log('--- Questions by Grade ---'); console.table(grades); await conn.end(); } check();